version: "3.9"

services:
  {{ docker_service_name }}:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      APP_NAME: {{ service_title }}
      APP_ENV: development
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      LOG_LEVEL: INFO